<div class="row">
    {{#with pelicula}}
    <h1>{{title}}</h1>
</div>
<div class="row">
    <div class="col-md-6 detailContainer">
        <img src="{{../URL_BASE}}{{poster_path}}" alt="" class="img-fluid">
        <h1 class="ml-4"><i class="fas fa-vote-yea" title="Nota Película: {{vote_average}}"></i>{{vote_average}}</h1>

        <form action="{{id}}/like" method="POST">
        <button class="btnPropio" type="submit">
            <i class="far fa-heart"></i></button>
        </button>
        </form>

    </div>

    <div class="col-md-6 detailContainer">
        <p>{{overview}}</p>
    </div>
    {{/with}}
</div>

